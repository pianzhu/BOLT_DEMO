[project]
name = "bolt-demo"
version = "0.0.0"
description = "Demo project skeleton (strands-agents + FastAPI)"
requires-python = ">=3.11"
dependencies = ["dashscope>=1.20.0"]

# Dependency management tool: uv
